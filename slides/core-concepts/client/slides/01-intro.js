import React, { PropTypes } from 'react';

/**
 * @module Intro
 */
export const Intro = React.createClass({
  render() {
    return (
      <div>
        <span className='temp'>Intro</span>
      </div>
    );
  },
});

export default Intro;
